---
title: "fea-docs — Changelog"
---

# fea-docs — Changelog

Scope-numbered directories under `docs/` map here. Each scope holds its own `map.md` (decision map) with `prd/` (requirements) and `build/` (implementation tickets).

| Scope | Title | Status | Date | Notes |
|-------|-------|--------|------|-------|
| 01-core/build | 01 — CLI scaffold & build basics | resolved | 2026-08-20 | `fea-docs build` CI-ready basis: recursive scan, `.gitignore` (root + subdirs), out-dir/`.git`/`node_modules` exclusions, symlink skip, empty-tree failsafe, help/error/exit-code rules; conventions in `CONVENTIONS.md` |

