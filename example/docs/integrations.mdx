---
title: Integrations
publish: engineering
---

import Counter from '@react-lib/Counter.tsx';
import Toggle from '@svelte-lib/Toggle.svelte';
import Accordion from '@astro-lib/Accordion.astro';

# Integrations

This page renders live components from three different component libraries.

## React — Counter

A stateful counter built with React and TypeScript.

<Counter client:load />

## Svelte — Toggle

A boolean toggle built with Svelte.

<Toggle client:load />

## Astro — Accordion

A native `<details>`-based accordion built as an Astro component.

<Accordion title="What is fea-docs?">
  `fea-docs` turns any directory of Markdown and MDX files into a Starlight
  docs site — zero config required.
</Accordion>
