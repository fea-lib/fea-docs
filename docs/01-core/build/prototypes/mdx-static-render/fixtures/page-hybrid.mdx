import {Counter, Effect} from "./Hybrid.mjs";

# Hybrid page

This page contains two `hybrid` components. Static HTML still renders; the page
ships their code in the page chunk.

<Counter renderMode="hybrid" />
<Effect renderMode="hybrid" />